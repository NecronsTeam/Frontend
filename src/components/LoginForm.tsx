import React, { FC, useContext, useState } from 'react';
import { Context } from '../main';

const LoginForm: FC = () => {
  return (
    <div>
      works
    </div>
  );
};

export default LoginForm;